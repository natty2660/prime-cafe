# Prime Cafe Environment Configuration
PORT=3000
NODE_ENV=development

# Admin Authentication
ADMIN_PASSWORD=primecafe2026
ADMIN_SECRET=prime_cafe_secret_key_2026

# Public Base URL for QR generation (optional)
PUBLIC_BASE_URL=

# PostgreSQL Database (Optional - falls back to local data/prime_cafe_db.json if not set)
DATABASE_URL=

# Gemini API (if generative AI features are enabled)
GEMINI_API_KEY=
